using System;
using System.Collections.Generic;
using System.IO;
using System.Text.Json;
using DotTiled;

namespace DotTiled;
public  class CustomTypeImporter
{
	//public  List<ICustomTypeDefinition> ImportCustomTypes(string path_to_json) {
	//	using (StreamReader file = File.OpenText(path_to_json)) {
	//		var serializer = new JsonSerializer();
	//		var dataFromJsonSerializer = (IEnumerable<IEnumerable<string>>)serializer.Deserialize(file, typeof(MyData));

	//		// dataFromJsonSerializer now holds your JSON data from file
	//	}
	//}
}
